import React from 'react'

import Card from './Card'

function Board() {
    const cards = [];

    return (<div className='w-per-7 h-per-16 bg-c-light'>
        {
            cards.map((card) => {})
        }
    </div>)
}

export default Board